# ربات سیگنال‌دهی و معامله‌گر مجازی (پیپر ترید)

## این ربات چیکار می‌کنه؟
- هر ۱۵ دقیقه قیمت چند ارز رمزنگاری‌شده رو از صرافی‌های رایگان (بایننس/کوکوین/اوکی‌اکس، بدون نیاز به API Key) می‌گیره.
- روند رو با **تئوری داو** (سقف/کف بالاتر یا پایین‌تر + تایید حجم) مشخص می‌کنه.
- سطوح **حمایت و مقاومت** رو از نقاط سوینگ قیمت پیدا می‌کنه.
- فقط وقتی روند + نزدیکی به سطح + حداقل نسبت ریسک‌به‌ریوارد (پیش‌فرض ۱:۲) هر سه هم‌زمان جور بشن، سیگنال صادر می‌کنه. همین باعث می‌شه تعداد سیگنال‌ها کم و انتخابی باشه، نه پشت‌سرهم.
- روی هر سیگنال یک معامله **کاملاً مجازی** (بدون پول واقعی، بدون اتصال به صرافی برای سفارش) باز می‌کنه و طبق SL/TP می‌بندتش.
- یک داشبورد وب سبک روی پورت ۵۰۰۰ نشون می‌ده: روند/قیمت/سطوح هر نماد، سیگنال فعلی، معاملات باز، تاریخچه، درصد وین، و منحنی موجودی.

⚠️ **این ابزار مشاوره مالی نیست.** حمایت/مقاومت و تئوری داو ابزار فیلتر نویز هستن، نه پیش‌بینی قطعی بازار. قبل از اینکه به سیگنال‌هاش اعتماد کنی، حداقل چند هفته بذار توی حالت مجازی نتیجه بده و آمار واقعیش (win rate, تعداد معامله) رو ببین.

---

## نصب روی VPS اوبونتو

```bash
# ۱. آپدیت سیستم و نصب پیش‌نیازها
sudo apt update && sudo apt install -y python3-venv python3-pip

# ۲. فایل‌های پروژه رو توی هوم دایرکتوری آپلود کن (مثلاً با scp یا git)
cd ~/tradingbot

# ۳. ساخت محیط مجازی پایتون (برای تمیز و سبک ماندن سیستم)
python3 -m venv venv
source venv/bin/activate

# ۴. نصب وابستگی‌ها
pip install -r requirements.txt

# ۵. تست اجرای دستی (Ctrl+C برای توقف)
python3 bot.py
```

اگه اجرا شد، برو به آدرس `http://IP-VPS-تو:5000` توی مرورگر تا داشبورد رو ببینی.

### اجرای دائمی (که با ری‌استارت سرور هم بالا بیاد)

```bash
# فایل سرویس رو کپی کن (به‌جای youruser اسم یوزر لینوکس خودت رو بذار)
sudo cp tradingbot.service /etc/systemd/system/tradingbot.service
sudo sed -i 's/%i/youruser/g' /etc/systemd/system/tradingbot.service
sudo systemctl daemon-reload
sudo systemctl enable tradingbot
sudo systemctl start tradingbot

# چک وضعیت و لاگ‌ها
sudo systemctl status tradingbot
sudo journalctl -u tradingbot -f
```

فایل سرویس مصرف رم رو به ۳۰۰ مگابایت و CPU رو به ۵۰٪ محدود کرده تا روی یه VPS سبک هم مشکلی پیش نیاد.

### باز کردن پورت ۵۰۰۰ (اگه فایروال فعاله)
```bash
sudo ufw allow 5000/tcp
```

اگه دوست داری آدرس بدون `:5000` باشه یا HTTPS داشته باشی، یه Nginx reverse proxy جلوش بذار (اختیاریه، ربات بدون اون هم کار می‌کنه).

---

## تنظیمات قابل تغییر (فایل `config.py`)

| متغیر | توضیح |
|---|---|
| `SYMBOLS` | لیست ارزهایی که رصد می‌شن |
| `TIMEFRAME` | تایم‌فریم کندل (پیش‌فرض 15m) |
| `MIN_RISK_REWARD` | حداقل نسبت ریسک به ریوارد قابل قبول |
| `PROXIMITY_PCT` | چقدر قیمت باید به حمایت/مقاومت نزدیک باشه تا سیگنال بده |
| `RISK_PER_TRADE_PCT` | درصد سرمایه مجازی که در هر معامله ریسک می‌شه |
| `VIRTUAL_BALANCE_START` | موجودی اولیه حساب مجازی |
| `SCAN_INTERVAL_MINUTES` | هر چند دقیقه دنبال سیگنال جدید بگرده |

بعد از تغییر `config.py`، سرویس رو ری‌استارت کن:
```bash
sudo systemctl restart tradingbot
```

## ریست کردن حساب مجازی
کافیه فایل دیتابیس رو پاک کنی و سرویس رو ری‌استارت کنی:
```bash
sudo systemctl stop tradingbot
rm tradingbot.db
sudo systemctl start tradingbot
```

## پیشنهادهای بعدی (اگه خواستی اضافه کنیم)
- **بک‌تست روی دیتای تاریخی** برای دیدن عملکرد استراتژی روی چند ماه گذشته قبل از اعتماد کردن بهش.
- **اعلان تلگرام** برای هر سیگنال جدید (اینستنت خبردار بشی بدون نیاز به چک کردن مدام داشبورد).
- **مولتی تایم‌فریم**: تایید سیگنال ۱۵ دقیقه‌ای با روند تایم‌فریم بالاتر (مثلاً ۱ ساعته) برای کاهش سیگنال‌های اشتباه.
- **فیلتر نوسان بازار (ATR نسبی)** برای رد کردن دوره‌های خیلی کم‌نوسان یا خیلی پرنوسان.
